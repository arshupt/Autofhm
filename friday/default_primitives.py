from unit_transformations import log_transfom, reciprocal_transform, sqrt_transform, power_transform

DEFAULT_PRIMITIVES = {
    "log_transfom" : log_transfom,
    "reciprocal_transform" : reciprocal_transform,
    "sqrt_transform" : sqrt_transform,
    "power_transform" : power_transform
}